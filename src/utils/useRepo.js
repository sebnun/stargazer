import useSWR from 'swr'

const useRepo = (repo) => {
    // const fetcher
    // const { data, error } = useSWR(repo, fetcher)
  
    // return {
    //   user: data,
    //   isLoading: !error && !data,
    //   isError: error
    // }
  }